<header class="header1 header">
    <div class="left" id="user-info">
        <% if (user) { %>
            <img src="<%= user.imageUrl %>" alt="User Image" class="user-img">
        <% } %>
    </div>
    <div class="right">
        <input type="text" placeholder="Recherche...">
        <select>
            <option>FR</option>
            <option>EN</option>
        </select>
    </div>
</header>